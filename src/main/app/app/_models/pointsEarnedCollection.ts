import { PointsEarned } from "./pointsEarned";
import { Student } from "./student";

export class PointsEarnedCollection {
    eventDate?: string;
    Student?: Student;
    points?: PointsEarned[];
}